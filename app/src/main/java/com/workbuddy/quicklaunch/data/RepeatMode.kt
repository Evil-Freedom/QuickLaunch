package com.workbuddy.quicklaunch.data

/**
 * 重复模式常量。
 * 消除 "daily" / "weekdays" / "weekend" / "custom" / "once" 魔法字符串。
 */
object RepeatMode {
    const val DAILY = "daily"
    const val WEEKDAYS = "weekdays"
    const val WEEKEND = "weekend"
    const val CUSTOM = "custom"
    const val ONCE = "once"
}
